git add .
git commit -m "first commit"
git branch -M main
girt remote add origin https://github.com/mehabubdot/HTML.git
git oush -u origin main

git config --global user.email "mehabubdot@gmail.com"
git config -- global user.name "Mehabubdot"

for cloning

git clone https"//github.com/git username/repository name.git